# Celery task queue environment
#
# Services:
#   api      - FastAPI app, publishes tasks via Celery
#   worker   - Celery worker, consumes tasks, reads/writes MinIO
#   rabbitmq - Message broker (Celery transport)
#   redis    - Result backend (Celery results cache)
#   minio    - S3-compatible object storage
#
# Only api:8000 exposed for host access.
# Default credentials in .env.
services:
  api:
    build:
      context: ./src
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      BROKER_URL: amqp://${RABBITMQ_USER:-user}:${RABBITMQ_PASSWORD:-pass}@rabbitmq:5672//
      BACKEND_URL: redis://:${REDIS_PASSWORD:-pass}@redis:6379/0
    depends_on:
      - rabbitmq
      - redis
    restart: unless-stopped

  worker-upload:
    build:
      context: ./src
      dockerfile: workers/upload/Dockerfile
    environment:
      BROKER_URL: amqp://${RABBITMQ_USER:-user}:${RABBITMQ_PASSWORD:-pass}@rabbitmq:5672//
      BACKEND_URL: redis://:${REDIS_PASSWORD:-pass}@redis:6379/0
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-root}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-password}
    depends_on:
      - rabbitmq
      - redis
      - minio
    restart: unless-stopped

  worker-download:
    build:
      context: ./src
      dockerfile: workers/download/Dockerfile
    environment:
      BROKER_URL: amqp://${RABBITMQ_USER:-user}:${RABBITMQ_PASSWORD:-pass}@rabbitmq:5672//
      BACKEND_URL: redis://:${REDIS_PASSWORD:-pass}@redis:6379/0
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-root}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-password}
    depends_on:
      - rabbitmq
      - redis
      - minio
    restart: unless-stopped

  # Message Queue
  rabbitmq:
    image: rabbitmq:3
    #ports:
    #  - "5672:5672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-user}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-pass}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Storage
  redis:
    image: redis:alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:-pass}
    #ports:
    #  - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "redis-cli",
          "--no-auth-warning",
          "-a",
          "${REDIS_PASSWORD:-pass}",
          "ping",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    #ports:
    #  - "9000:9000"
    #  - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-root}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-password}
    volumes:
      - minio_data:/data

volumes:
  rabbitmq_data:
  redis_data:
  minio_data:

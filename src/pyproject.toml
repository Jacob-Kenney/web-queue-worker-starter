[project]
name = "api"
version = "0.1.0"
description = "Queue worker starter API"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "celery",
    "redis",
    "s3fs",
    "pydantic",
]
